name: RepoReady

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  repoready:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run RepoReady
        run: node packages/cli/bin/repoready.js --markdown >> "$GITHUB_STEP_SUMMARY"
